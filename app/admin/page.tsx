import AdminContent from "./AdminContent";

export const metadata = {
  title: "Order Dashboard — SnackIt",
  robots: { index: false, follow: false },
};

export default function AdminPage() {
  return <AdminContent />;
}
