import BuildYourOwnContent from "./BuildYourOwnContent";

export const metadata = {
  title: "Build Your Own — SnackIt",
  description:
    "Pick a base, a bread format, and a flavor toss. Build your own vegetarian sandwich, pita pocket, or sub roll — every combination gets a regional story.",
};

export default function BuildYourOwnPage() {
  return <BuildYourOwnContent />;
}
