import LoginForm from "./LoginForm";

export const metadata = {
  title: "POS Sign in — SnackIt",
  robots: { index: false, follow: false },
};

export default function PosLoginPage() {
  return <LoginForm />;
}
