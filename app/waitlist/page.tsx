import WaitlistContent from "./WaitlistContent";

export const metadata = {
  title: "Join the Waitlist — SnackIt",
  description:
    "Be the first to know when SnackIt opens near you — Nadiad, Gujarat or Calgary, Alberta.",
};

export default function WaitlistPage() {
  return <WaitlistContent />;
}
